#python file that can import another import into current file with full access inside cuent file
import use_ful_tools #python gets file

print(use_ful_tools.roll_dice(10)) #the "." give access to what's in file; illustration of rolling a dice

#"PIP" used to instal python modules
